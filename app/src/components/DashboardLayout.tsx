"use client";
import React from 'react';

// Este componente não é mais usado diretamente
// O layout está em src/app/(authenticated)/layout.tsx
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
